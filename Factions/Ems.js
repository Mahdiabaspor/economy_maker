const healingCost = 1500
const RespawnHospital = 3000
const woundedTime = 180 // seconds shown on the death screen before auto hospital respawn
export const EmsConfig = {
    healingCost,
    RespawnHospital,
    woundedTime,
}
