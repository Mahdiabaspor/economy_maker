
const dmvMultiplier = 1;

export const licenseDmvConfig = {
    VEHICLE:( 7000 * dmvMultiplier).toFixed(0),
    CDL_A:( 10000 * dmvMultiplier).toFixed(0),
    CDL_B:( 12000 * dmvMultiplier).toFixed(0),
    HPL:( 80000 * dmvMultiplier).toFixed(0),
    PPL:( 120000 * dmvMultiplier).toFixed(0),
};